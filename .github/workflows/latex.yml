name: LaTex Compile and Push

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2

      - name: Compile CV
        uses: xu-cheng/latex-action@v2
        with:
          root_file: |
            cv_en/cv_en.tex
            cv_cn/cv_cn.tex
          work_in_root_file_dir: true
          latexmk_use_xelatex: true

      - name: Setup repository
        run: |
          ls -l ${GITHUB_WORKSPACE}
          ls -l ${GITHUB_WORKSPACE}/cv_cn
          ls -l ${GITHUB_WORKSPACE}/cv_en

          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git clone -b master https://github.com/beiyuouo/beiyuouo.github.io homepage

          cp ${GITHUB_WORKSPACE}/cv_cn/cv_cn.pdf homepage/pdf/cv_cn.pdf
          cp ${GITHUB_WORKSPACE}/cv_en/cv_en.pdf homepage/pdf/cv_en.pdf

      - name: Update CV
        uses: ad-m/github-push-action@master
        with:
          directory: homepage
          github_token: ${{ secrets.PUBLISH_KEY }}
          branch: master
          force: true
          repository: beiyuouo/beiyuouo.github.io
